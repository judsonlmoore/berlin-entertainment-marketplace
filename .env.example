# Public base URL used by agreement and email provider callbacks in later phases.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Future provider integrations. The MVP uses explicit sandbox placeholders only.
ESIGN_PROVIDER=mock
ESIGN_API_KEY=
OBJECT_STORAGE_BUCKET=
