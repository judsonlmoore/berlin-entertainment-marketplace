# Environment values; keep the names-only template.
.env*
!.env.example

# Dependencies and generated application output once implementation begins.
node_modules/
.next/
out/
dist/
coverage/
.turbo/
*.tsbuildinfo
# Regenerated by next dev / next build / next typegen (path flips between .next/types and .next/dev/types).
next-env.d.ts

# Vercel and local tooling state.
.vercel/
.vite/
.vinext/
.wrangler/
.DS_Store
*.log

# Local scratch/deliverable directories.
outputs/
work/
.data/
.vercel
.gstack/
